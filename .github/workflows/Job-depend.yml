name: Workflow

run-name: Job Dependencies

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Job
        run: echo "Build Job"

  Test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test Job
        run: echo "Test Job"
