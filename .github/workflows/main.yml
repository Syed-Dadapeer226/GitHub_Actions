name: Second CI

on:
  push:

jobs:
  demo-job:
    runs-on: ubuntu-latest
    steps:
      #----------------------
      # First Step
      #----------------------
      - name: Step-1
        run: echo "First Step"
      #----------------------
      # First Step
      #----------------------
      - name: Step-2
        run: echo "Second Step"
